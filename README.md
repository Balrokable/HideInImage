Allows to hide data in png-images. 
First select an image, then enter text or select a file as payload and inject it.
The saved image contains the data which can be extracted after selecting it again.
